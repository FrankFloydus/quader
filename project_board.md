# Project Board

This board tracks active work only. Completed, canceled, or obsolete entries are
removed only when the user explicitly asks for that board operation.

## Backlog

#13 [priority:high][type:enhancement][area:viewport] Add Godot-style transform gizmos to the viewport - Add move, rotate, and scale viewport gizmos modeled on Godot, with camera-scaled handles, analytic picking, drag previews, snapping, commit/cancel behavior, and a Quader-specific uniform-scale center cube.
  Brief: Source: Final Task Intake Report for Godot-style transform gizmos. Active plan: agents/plans/audit_20260704_full_architecture_master.md. Duplicate of active #13; no new board task. Source refs: C:\Users\Drako\Desktop\_QUADER_DEV\resources\_SOURCE_CODE\godot-master | editor/scene/3d/node_3d_editor_plugin.cpp:117-133 constants; 1426-1481 edit setup; 1515-1765 hit selection; 1811-1866 transform computation; 2240-2750 input routing/commit/cancel; 4157-4248 rotation angle pie overlay; 5027-5150 camera-based gizmo_scale and visibility; 6170-6584 transform apply/update math; 7400-7456 gizmo center/basis; 8326-8955 move/rotate/scale mesh construction; editor/scene/3d/node_3d_editor_plugin.h:336-407 transform modes/edit state; 633-673 tool modes and mesh/material ownership; 951-979 gizmo transform/snap/update APIs; editor/settings/editor_settings.cpp:636-637 touchscreen handle scale; 990-993 manipulator size/opacity/show-during-rotation/view-plane-rotation-scale settings. Implementation must begin with follow-up architect planning inside this task and proceed after or coordinated with #9 Crimson overlay/picking slices. Do not implement directly in MainWindow, ViewportWidget, or prototype renderer paths. #7 provides document/command/tool shell groundwork, but real 3D gizmo interaction still needs new tool, math, preview, command, UI input, overlay, and picking contracts. #8 is adjacent for UI models/settings if configurable settings UI is included. Scope: visual move, rotate, and scale gizmos modeled on Godot 3D editor, including camera-based constant-screen scaling, picking distance/sensitivity, drag/use sensitivity, snapping, commit/cancel preview behavior, local/global axes, multi-selection pivot, view-plane or trackball behavior where enabled, and rotation angle pie visualization. Match Godot behavior and structure as closely as practical without unsafe verbatim copying. Quader divergence: scale center handle is a small cube for uniform XYZ scale instead of Godot center drag policy. Likely contracts: modeling_tools editor input extensions; transform_gizmo model, settings, hit tester, drag session, handle IDs, modes, axes, and parts; semantic 3D tool preview data; batch transform command if needed; math/geometry ray-plane, ray-sphere, ray-box, projection, and rotation helpers; UI viewport event routing and W/E/R plus V action behavior; Crimson overlay commands and picking integration. Renderer rules: gizmos are unlit overlays only, rotation pie is Crimson overlay geometry, CPU analytic hit testing should match rendered gizmo data, and no lit, post, HDR, bloom, fog, shadow, or runtime shader compilation path is allowed. Tests-policy applies; planners, workers, and reviewers must read agents/tests-policy.md. Expected tests: transform gizmo math, hit tolerances, center cube uniform scale, drag preview, snapping, cancel/commit, command undo/redo, UI event/action translation, Crimson overlay/picking separation, and geometry/math degenerate cases. Verification: cmake --build --preset qt-mingw-debug; ctest --preset qt-mingw-debug; cmake --build --preset qt-mingw-debug --target check_architecture; cmake --build --preset qt-mingw-debug --target deploy; python tools/project_board.py validate.
  Final owner: multi-domain: software finalizes tool, command, document, preview, and math boundaries; renderer owns Crimson overlay, picking, gizmo rendering, and renderer tests; UI owns viewport input, actions, shortcuts, settings, and UI tests; software arbitrates cross-domain final approval with renderer and UI authority reviews
  Plans: agents/plans/audit_20260704_full_architecture_master.md
#18 [priority:high][type:enhancement][area:architecture] Add folder-based projects, multi-scene sessions, and texture asset import - Add project-level ownership so one folder-based project can contain multiple switchable scenes and copied project-local texture assets.
  Brief: Architecture-sensitive cross-domain task. Source: Final Task Intake Report for folder-based projects, multi-scene sessions, and texture asset import. Active plan: agents/plans/audit_20260704_full_architecture_master.md. Not a duplicate of #10: #10 covers later importer/exporter and asset-boundary work, while this task owns project/session ownership, multi-scene switching, scene-tab UI, project-local texture import, and asset catalog infrastructure. Must coordinate with #10 for src/io and asset-boundary direction. Must not interfere with active #9; do not add Crimson texture upload or live rendering until #9 is approved or PM sequences a non-conflicting renderer slice. Implementation must start with a follow-up architect implementation plan before code work. Build folder-based project/session infrastructure where a project owns multiple scenes, each scene has its own Document and CommandHistory, and the UI can switch active scenes inside one project. First slice must include project-local texture import: copy user-selected texture files into the project asset folder, register them in an asset catalog with stable AssetId, project-relative path, diagnostics, intended role/color-space metadata, and import status. Do not implement project/session logic directly in MainWindow, ViewportWidget, or Crimson. File dialogs stay in UI services; project/asset services perform validation, copying, catalog mutation, and diagnostics. Likely files/classes/contracts: src/project Project, ProjectSession, ProjectScene, SceneId, ProjectManifest, ProjectDocumentSlot, ProjectAsset, AssetId, AssetRegistry; src/io ProjectStorage, ProjectPathPolicy, TextureImportService, AssetImportDiagnostic; AppServices, UiContext, DocumentUiController, EditorActionStateProvider, EditorActionController, ToolContext; SceneTabModel, SceneTabStrip, ProjectUiController, AssetUiController, AssetItemModel, AssetPanel, DialogService, PanelId::Assets, and actions for project/scene/texture import. Renderer-facing future contract: project AssetIds map to renderer-safe texture upload/cache handles inside Crimson, with graphics-runtime handles confined to src/crimson/gpu. No material editing UI, PBR expansion, glTF material import, document-backed material assignment, or live texture rendering unless separately authorized and sequenced with #9/#10. Edge cases: dirty scene switching and close-project/exit prompts, undo/redo following active scene command history, stable SceneId use instead of row indexes or paths, texture name collisions, duplicates/hash, unsupported formats, path traversal, already-inside-project files, missing sources, partial-copy failures, and no catalog mutation on failed import. agents/tests-policy.md applies. Expected tests: project manifest create/load and malformed manifests, multi-scene active switching, per-scene dirty state and command history, asset ID stability, texture copy/import, collision handling, project-relative paths, duplicate imports/dedupe, no catalog mutation on failed import, QAbstractItemModelTester coverage for SceneTabModel and AssetItemModel, action-state and fake dialog-service tests, and renderer tests only for texture role/color-space mapping/no include leaks unless texture upload is explicitly in scope. Verification: cmake --build --preset qt-mingw-debug; ctest --preset qt-mingw-debug; cmake --build --preset qt-mingw-debug --target check_architecture; python tools/project_board.py validate; if user-visible runtime behavior changes, cmake --build --preset qt-mingw-debug --target deploy. Image refs: C:\Users\Drako\AppData\Local\Temp\codex-clipboard-06813de6-43f1-4f87-81fa-c08ea1312d5c.png.
  Final owner: multi-domain; software final owner for project/session/document/I/O/asset boundaries, UI authority for tab strip/actions/dialogs/panels/models, renderer authority for Crimson texture contracts if texture upload/render use is added
  Plans: agents/plans/audit_20260704_full_architecture_master.md
## In Progress

## In Review

## Bugs

#20 [priority:high][type:bug][area:ui] Fix diagnostics panel tree expansion flicker - The dockable Diagnostics panel tree cannot keep sections expanded because routine viewport diagnostics refreshes reset the model while rendering.
  Created: 2026-07-05T07:27:28+02:00
  Brief: Source: Final Task Intake Report for diagnostics panel tree expansion flicker. UI follow-up from #11 diagnostics panel/status presentation, not a duplicate of #19. Likely cause is high-frequency frame_stats_changed -> ViewportDiagnosticsService::refresh() -> diagnostics_changed() -> DiagnosticsItemModel::reload_from_service() using beginResetModel(), which wipes QTreeView expansion state. Likely files: src/ui/panels/diagnostics_panel.*, src/ui/models/diagnostics_item_model.*, src/ui/services/viewport_diagnostics_service.*, src/ui/qt_app/main_window.cpp, viewport cadence context in src/ui/viewport/viewport_controller.cpp and viewport_widget.cpp. Intended fix: preserve expansion/selection during routine updates; reduce unnecessary diagnostics_changed emissions and/or update existing rows with dataChanged() when topology is unchanged; reserve full reset for topology changes. Keep status updates frequent if needed, keep copy selected/all behavior, do not move logic into Crimson, do not scrape widgets, and do not replace the model/view architecture with QTreeWidget. Risks: per-frame counters, legitimate topology changes, visible panel state, UI diagnostics must remain free of Crimson GPU/runtime includes. Tests-policy applies; agents/tests-policy.md is required for any test planning, modification, or review. Suggested tests: expand a diagnostics tree section, trigger repeated stable diagnostics refreshes, assert expansion remains and no reset-driven flicker occurs. Verification: cmake --build --preset qt-mingw-debug; ctest --preset qt-mingw-debug -R ui_diagnostics_model_tests|ui_viewport_diagnostics_service_tests|ui_shell_tests; ctest --preset qt-mingw-debug; cmake --build --preset qt-mingw-debug --target check_architecture; cmake --build --preset qt-mingw-debug --target deploy; python tools/project_board.py validate. Manual: launch quader.exe, open View > Panels > Diagnostics, expand/collapse Summary, Render Passes, Counters, and Diagnostics while viewport renders; sections should stay chosen without flicker.
  Freshness: [status:needs-refresh][checked:never] Awaiting start-gate revalidation.
  Final owner: UI owner: diagnostics panel, diagnostics item model, viewport diagnostics service, MainWindow diagnostics refresh wiring, and UI diagnostics regression tests
  Plans: agents/plans/audit_20260704_full_architecture_master.md

#21 [priority:high][type:bug][area:viewport] Restore fly navigation mouse capture in the Qt viewport - Right-button fly navigation enters Fly mode and routes movement keys, but the Qt viewport does not capture, hide, warp, or feed relative mouse deltas while flying.
  Created: 2026-07-05T07:27:49+02:00
  Brief: Source: Final Task Intake Report for fly navigation mouse capture parity. Not a duplicate of #4, #8, #11, or #19. Source refs: `C:\Users\Drako\Desktop\quader-windows\quader-app` | `INPUT_KEYBINDINGS.md:40-42,100-117,168-173`; `agents/viewport-tools.md:50-61`; `src/viewport/host_viewport_contract_adapter.cpp:220-223,249-272`; `src/viewport/viewport_input_types.hpp:86-100`; `src/viewport/viewport_input_router.hpp:52-64,73-79`; `src/viewport/viewport_input_router.cpp:104-181,259-270`; `tests/core_tests.cpp:6500-6540`; `src/ui/ui_platform_adapter.hpp:37-43`; `src/app/app.cpp:84-99,112-118`; `src/ui/ui_context.hpp:70-83`; `src/ui/ui_system.cpp:487-497,681-695,811-815,1063-1095`; `src/ui/viewport_panel.cpp:1212-1222,1638-1654,1711-1723`. Current findings: src/ui/viewport/viewport_widget.cpp handles mouse press/move/release and W/A/S/D forwarding but does not grab/release mouse, hide/restore cursor, warp cursor, maintain relative capture state, or fully consume fly keys while active. ViewportController starts right-button Fly in perspective and Pan in orthographic; ViewportCameraController uses absolute widget positions and has no relative captured delta path; viewport_types.hpp has NavigationMode::Fly but no input/result contract for fly capture state. Intended fix: keep Qt-specific capture in ViewportWidget and navigation interpretation in viewport controller/camera classes; add a small UI-facing state/result contract so perspective right-drag Fly requests cursor capture, records an anchor, hides cursor, captures input, uses relative deltas or warp-back mouselook, and restores on release, cancellation, focus/window loss, hidden/destructor. Consume W/A/S/D while Fly is active; leave middle orbit/pan, orthographic right-button pan, splitter dragging, and renderer behavior unchanged. Risks: HiDPI, multi-monitor global positions, synthetic warp moves, focus/window deactivate cleanup, orthographic right-button pan, platform cursor-warp limitations. Tests-policy applies; agents/tests-policy.md is required for any test planning, modification, or review. Verification: cmake --build --preset qt-mingw-debug; ctest --preset qt-mingw-debug; cmake --build --preset qt-mingw-debug --target check_architecture; cmake --build --preset qt-mingw-debug --target deploy; python tools/project_board.py validate. Manual: launch quader.exe, hold right mouse in perspective viewport, verify cursor hides/captures, mouselook continues at window/screen edges, W/A/S/D movement continues, wheel adjusts fly speed, and release or focus loss restores cursor/input cleanly.
  Freshness: [status:needs-refresh][checked:never] Awaiting start-gate revalidation.
  Final owner: UI owner: src/ui/viewport/viewport_widget.*, src/ui/viewport/viewport_controller.*, src/ui/viewport/viewport_camera_controller.*, src/ui/viewport/viewport_types.hpp, and tests/unit/ui/viewport_tests.cpp
  Plans: agents/plans/audit_20260704_full_architecture_master.md
